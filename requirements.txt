# Lightweight requirements for Railway free tier (under 4GB image size)
Flask==3.1.1
Flask-Session==0.8.0
gunicorn==23.0.0
python-dotenv==1.1.1
openai==0.28.0
requests==2.32.3
numpy==2.2.5
# Removed heavy ML dependencies to fit 4GB limit
# Using simple keyword matching instead of transformers/pytorch
